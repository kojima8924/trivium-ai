# DEMO.md — 3 分のデモ台本

面談で「実際に触れる Web サービス」として見せるための手順。**壊れにくさ優先**で、決定論的に動く経路を軸にする。

> **実測値（seed 直後・決定論）**: READ **Lv.6（60.0）** / WRITE **Lv.4（40.0）** / LOGIC **Lv.7（76.3）**、XP **1139**、ランク **Grammarian（文法家）**、🔥 連続 **3 日**、デイリーミッション達成 **6 日**、実績 **25 個**、スコアの時系列グラフ **10 点**。
> デモ課題 `code-006`（二分探索の終了条件・難易度 8）をヒント 1 回で正答すると **LOGIC Lv.7 → Lv.8（76.3 → 80.0）**、XP +64、実績 **「立て直し」と「論理の番人（LOGIC Lv.8）」** が大きなトーストで解除。seed の日時は JST 正午に固定しているので、いつ seed しても同じ値になる（決定論）。
>
> **seed の 10 日間**: 10 日前にやさしい 2 問で開始 → 9 日前に初のミッション達成 → 8 日前に読解で失敗（ヒント 3 回でも届かず）→ 7 日前は**複合問題（READ+WRITE）＋ LOGIC の 2 問**でミッション達成 → 6 日前はお休み → 4 日前に難易度 9 で失敗 → 3 日前に**同じ課題へリベンジ成功** → 直近 3 日は連続達成。ヒント 0 / 1 / 2 回の正解と、ヒント 3 回でも未達の記録が混ざっている。

## 登場人物（AI の 4 人格）

| 系統 | 名前 | 既定の口調 | 役割 |
|---|---|---|---|
| READ | ヨミ（男） | 丁寧・古風 | 黄泉の図書館の司書。本文の言葉で根拠を確かめさせる。口癖「本文に、書いてある。」 |
| WRITE | フミ（女） | 先輩 | 新聞部の赤ペン先輩。主張を尊重し、構成と根拠だけを問う。口癖「で、根拠は？」 |
| LOGIC | ロゴス（男） | 厳格 | 論理の番人（白衣のプログラマ）。値を一つずつ追わせる。口癖「値を追え。」 |
| ADVISOR（内部キー LEADER） | ミチ（女） | **ツンデレ** | 三叉路（trivium）の案内人。3 系統を横断して見て、最後は必ず次の一歩を 1 つ示す |

名前・口調（12 プリセット）・一人称・補足は `/settings` で変えられる。既定値は `src/config/trivium.config.ts`。

## 事前準備（当日 18:30 までに）

- [ ] `npm run preflight -- https://trivium.153.126.213.251.sslip.io` が全項目 OK（health / トップ / Google OAuth / 静的アセット / LINE 署名検証 / 認証ガード / HTTPS）
- [ ] `/api/health` の `ai.provider` が `openai`、`lastError` が `null`（誤答→ヒントは 2〜5 秒、正答→寸評+ADVISOR 更新は 5〜10 秒）。遅い／不安なら `AI_PROVIDER=mock` にすると即時応答のルールベースに切り替わる
- [ ] Google ログイン済み（デモログインは本番では無効。`DEMO_LOGIN_ENABLED=false`） → Dashboard で **デモデータを投入** 済み（三角形と XP カードが出ている）
- [ ] 直近で `code-006`（二分探索の終了条件）を解いていない（解いた場合は「デモデータで置き換える」でリセット）。seed 直後の値: READ Lv.6（60.0）/ WRITE Lv.4（40.0）/ LOGIC Lv.7（76.3）・XP 1139・連続 3 日
- [ ] 講評キャッシュを本番向けに生成済み（ログイン状態で `POST /api/demo/warm`、または `npm run warm-cache -- --concurrency 1`）。LINE の選択式が即答になる
- [ ] スマートフォンで `https://trivium.153.126.213.251.sslip.io` を開き、ログイン状態にしておく（LINE から開く導線用）
- [ ] LINE 公式アカウントを友だち追加済み・Rich Menu（上段 READ / WRITE / LOGIC = LINE で 1 問、下段 使い方 / Dashboard / PROFILE）が出ている
- [ ] LINE で「連携」→ 届いた URL を開いて **デモ用アカウントと連携済み**（LINE で「プロフィール」と送ると Flex カードが返る状態）
- [ ] 保険: デモログインは本番で無効（`DEMO_LOGIN_ENABLED=false`）。Google がこけたときだけ Coolify で `true`＋`DEMO_LOGIN_SECRET`（合言葉）を設定して Restart する（合言葉が無いと既存アカウントには入れない）。`/api/demo/warm` は `ADMIN_EMAILS` に自分のメールを入れたアカウントでのみ実行可
- [ ] ブラウザは 1 タブ・ズーム 125% 程度・通知オフ。答えの入力は全角でも通るが、念のため IME を半角に

## 台本（合計 約 3 分）

### 0:00–0:20 ホーム — 思想を一言で

**操作**: トップページを表示。

**話す**:
「Trivium です。コンセプトは *AI does not do the work for you. It helps you take the next step.* — AI は答えを書かない。一段だけヒントを出して、本人に次の一歩を踏ませます。READ・WRITE・LOGIC の 3 系統があって、それぞれに AI の人格（ヨミ・フミ・ロゴス）がいて、その上に ADVISOR のミチが乗っています。ミチはツンデレです。」

### 0:20–0:50 Dashboard — 既存プロフィール

**操作**: 「Dashboard を開く」。三角形と XP カードを指す。

**話す**:
「Google ログインすると学習状態はサーバに保存されるので、別端末でも同じプロフィールです。三角形は**能力の証拠**、XP は**行動の積み上げ**で、別の指標にしてあります。数値は LLM の気分ではなく、学習イベントから決定論的に集計しています。難易度は系統ごとに 1〜10、『難易度 d 以上で 7 割解けているか』で到達レベルが決まる。失敗は原因になった系統にしか効かないので、読み書きが絡む論理問題を落としても READ は下がらない。」

（各系統カードを指しながら）「文章のほうは AI の解釈です。LOGIC は手順の追跡が強いが構造化に余地がある、WRITE は推敲が少ない、など。」

（次の一歩を指して）「ADVISOR が 3 系統を横断して『次のおすすめ』を出します。」

### 0:50–2:00 LOGIC を 1 問 — 誤答 → 一段ヒント → 正答

**操作**: アドレスバーで `/learn/logic?task=code-006` を開く（「二分探索の終了条件」。難易度 8。決定論的に採点される）。上部の「AI に問題を作ってもらう」欄は、時間があれば「論理パズルを 1 問」と入れて作問を見せる。

**話す**: 「では実際に 1 問。二分探索で 7 を探します。このプログラムの出力は？」（正解は `-1`。`while lo < hi` が `lo == hi` の要素を調べずに終わるバグ）

**操作**: わざと `3` と入力して「答える」（「7 は index 3 にあるから 3」というありがちな誤答）。

**見せる**: 「AIが確認中…」（数秒）→ ロゴスの講評＋ **hint 1**（例: 「まず while の条件に注目しましょう。lo と hi が同じになったとき、ループが続くかを確認してみてください。」）

> AI の文言は毎回生成されるので一字一句は変わる。変わらないのは「答え（-1）を言わない」「誤りの箇所を特定して教えない」「ヒントは一段だけ」という契約。決定論採点が incorrect のときは AI が success と言っても retry に上書きされる

**話す**: 「答えは教えない。問い返しが一段だけ。ここが設計の肝です。ヒントは最大 3 回で、何回目のヒントで解けたかがそのまま証拠と XP になります。」

**操作**: `-1` と入力 →「答える」。

**見せる**: 「AIが確認中…」（5〜10 秒。寸評と ADVISOR を並列生成）→「正解（ヒント 1 回）」、解説、**LOGIC Lv.7 → Lv.8**、獲得 XP（+64）、実績解除のトースト（キャラの cheer 画像つき）、ミチの総合寸評が直近の 1 問に触れ、次のおすすめが更新される。

**話す**: 「この 1 回が learning event として記録されて、LOGIC の到達レベルが 7 から 8 に上がりました。難易度 8 の正答率がしきい値を超えた瞬間です。ヒント付きの成功は『ヒントなし成功』より弱い証拠なので、普段はここまで簡単には動きません。同時に XP と ADVISOR の総合寸評、次の課題も更新されています。」

### 2:00–2:20 Dashboard に戻る

**操作**: 「Dashboard で変化を見る」。

**話す**: 「XP が増えて今日のミッションの LOGIC に ✓ が付き、最近の学習の先頭に今の 1 問が載っています。実績『立て直し』と『論理の番人（LOGIC Lv.8）』も解除。3 系統そろうとデイリーミッション達成で、LINE に総評と今日の 1 冊が届きます。」

### 2:20–2:55 LINE — 4 人格と話す・その場で解く

**操作 A**: スマートフォンで LINE を開き、Rich Menu の **LOGIC**（または「今日の学習」と送る）をタップ → 担当キャラの吹き出しで選択式が Quick Reply 付きで届く（パス／Web で解く が左端。最初は難易度 2 から）→ ボタンで答える → ○ ✕ △ つきの講評（誤答なら一段ヒントと再提示。「ヒント」と送ってもよい）→ 少し遅れて「Lv / +XP」が届く（5 問ごとに担当とミチの一言）。

**話す**: 「LINE は入口です。選択式ならその場で解けて、同じ記録に積まれます。講評はキャッシュされるので 2 回目以降は即答。」

**操作 B**: 「ミチ、10 分くらい何かやりたい」と送る → ツンデレの案内役が記録を踏まえて 1 つ提案（別の人格に聞きたければ「ロゴス、〜」のように名前で呼ぶ。「僕の能力は？」でプロフィールカード）。

**話す**: 「4 人格それぞれが担当系統の観察メモと直近 10 往復の会話を覚え、他の担当とのやり取りや直近の課題も共有しています。今日の日付も知っていて、時事の話題なら Web 検索を使います。」

**操作 C（時間があれば）**: 「論理パズル出して」と送る → 「作っています…」→ 数秒で AI が作った選択式が届く。「作った問題も同じ 3 系統・同じヒント方針で評価されます。」

**操作 D（時間があれば）**: Rich Menu の **PROFILE** → 到達レベル・XP バー・ミッション・streak の Flex カード。

**操作 E（時間があれば）**: 「おすすめの本ある？」と送る → ミチが到達レベルと弱い小分類に合わせて教材 3 件を理由つきで提案（候補ごとに「開く」ボタン。カタログは実在の定番書・公式サイト 85 件。Dify ナレッジ設定時は検索スコアも加味）。

### 締め（10 秒）

「skills are local, learner is global。系統ごとの記憶と、それを束ねる案内役。AI 層は OpenAI を server-side から呼んでいて、落ちてもルールベースにフォールバックするので、デモも運用も止まりません。」

## 質問が来たときの補足

| 質問 | 答え |
|---|---|
| スコアはどう計算？ | `src/lib/scoring.ts`。難易度ベクトルと到達レベル（正答率 70% の最大難易度）。失敗はボトルネック系統のみに帰属。IRT は未実装 |
| XP との違いは？ | 三角形＝能力の証拠（下がることもある）、XP＝行動の積み上げ（減らない）。ミッション・streak・ランクは XP 側 |
| AI は何をしている？ | 講評・一段ヒント・寸評・観察メモ・総合寸評・会話・作問（解釈と生成）。採点は選択/短答は決定論、記述はルーブリック＋AI |
| どのモデル？ | OpenAI Responses API + structured outputs。採点・寸評・会話は `gpt-5.4-mini`、作問は `gpt-5.5`（`src/config/trivium.config.ts` の `MODELS`）。`AI_PROVIDER` で Dify / mock にも切替可。Dify 用 DSL も `dify/` に同梱 |
| AI が答えを書いてしまわない保証は？ | system policy 7 か条＋ヒントは事前に段階設計されたものから一段だけ＋決定論採点が確定しているときは AI の status を上書き |
| 人格は？ | 4 人格。名前・口調（12 プリセット）・一人称・補足を `/settings` で変更。人格は文体にだけ効き、採点には影響しない |
| 記憶は？ | 系統ごとの観察メモ（本人には見せない）と、LINE の会話 10 往復。数値はメモに書かせない |
| 個人情報は？ | 保存は表示名とメールのみ。LLM には内部 ID だけ渡す。API key は全部 server-side |
| 落ちたら？ | `LearningAIService` が Mock provider にフォールバック。`/api/health` の `ai.lastUsed` / `lastError` で確認可 |
| LINE と Google は紐づく？ | LINE で「連携」→ ワンタイム URL（単回・15 分）→ Web でログイン中に承認。「連携解除」でいつでも解除。連携しなくても LINE 単体で使える |
| 問題数は？ | 手書き 63 問（READ / WRITE / LOGIC 単独 51 + 複合 12）＋ 生成・検証済みストック 246 問（READ 70 / WRITE 70 / LOGIC 76 / 複合 30。問題タイプ 15 種。Python は実行で正解照合、他は独立ソルバー照合＋人力で解けるかの判定）＋ AI 作問。難易度 1 は誰でも解け、10 は上級者でも数分かかる。LINE で「LOGICで難易度8」と送るとストックから即出題、「writeで軽めに」で推薦より 2 段やさしく |
| 初期状態に戻せる？ | Dashboard の「初期状態に戻す」で全消去（人格・LINE 連携は残る） |

## 壊れたときの逃げ道

| 事象 | 対応 |
|---|---|
| Google ログイン失敗 | Coolify で `DEMO_LOGIN_ENABLED=true`＋`DEMO_LOGIN_SECRET` を設定して Restart → `/login` の「デモとして入る」（本番の既定は無効） |
| 三角形が空 | Dashboard の「デモデータを投入」 |
| `code-006` が「回答済み」で別の問題が出る | URL に `?task=code-006` を付ければ指定できる。seed をやり直せば未回答に戻る |
| AI の返答が遅い／エラー | OpenAI は 25 秒でタイムアウト → 自動で Mock に切替（アプリは止まらない）。待てないなら `AI_PROVIDER=mock` で Restart |
| LINE が返事しない | Web 側だけで完結させ、「LINE は入口」の説明はスクリーンショットで代替 |
| LINE の連携が切れている | LINE で「連携」と送り直す（15 分有効）。未連携でも会話と誘導は動く（出題・記録は連携が必要） |
