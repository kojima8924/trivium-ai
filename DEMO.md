# DEMO.md — 2〜3 分のデモ台本

面談で「実際に触れる Web サービス」として見せるための手順。**壊れにくさ優先**で、決定論的に動く経路だけを使う。

## 事前準備（当日 18:30 までに）

- [ ] `https://<公開URL>/api/health` が `{"status":"ok","db":"ok",...}` を返す
- [ ] デモ用 Google アカウントでログイン済み → Dashboard で **デモデータを投入** 済み（三角形が出ている）
- [ ] 直近で `code-003`（平均値のバグ）を解いていない（解いた場合は「デモデータで置き換える」で履歴をリセット。seed 直後の値は READ 72 / WRITE 57 / CODE 79）
- [ ] スマートフォンで `https://<公開URL>` を開き、ログイン状態にしておく（LINE から開く導線用）
- [ ] LINE 公式アカウントを友だち追加済み・Rich Menu が出ている
- [ ] LINE で「連携」→ 届いた URL を開いて **デモ用アカウントと連携済み**（LINE で「プロフィール」と送ると READ 72 / WRITE 57 / CODE 79 が返る状態）
- [ ] 保険: `DEMO_LOGIN_ENABLED=true` にしてあれば Google がこけても「デモとして入る」で続行できる
- [ ] ブラウザは 1 タブ・ズーム 125% 程度・通知オフ

## 台本（合計 約 2 分 55 秒）

### 0:00–0:20 ホーム — 思想を一言で

**操作**: トップページを表示。

**話す**:
「Trivium です。コンセプトは *AI does not do the work for you. It helps you take the next step.* — AI は答えを書かない。一段だけヒントを出して、本人に次の一歩を踏ませます。READ・WRITE・CODE の 3 領域があって、その上に LEADER という学習者全体を見るモデルが乗っています。」

### 0:20–0:50 Dashboard — 既存プロフィール

**操作**: 「Dashboard を開く」。三角形レーダーを指す。

**話す**:
「Google ログインすると学習状態はサーバに保存されるので、別の端末でも同じプロフィールです。この三角形が能力プロフィール。数値は LLM の気分ではなく、学習イベントから決定論的に集計しています。ヒントなし成功 1.0、ヒント 1 回 0.8、失敗 0.2、難易度と新しさで重み付け。だから一回の失敗で『苦手』とは断定しない。」

（各 domain カードを指しながら）「文章のほうは AI の解釈です。CODE はトレースが強いが設計の言語化に余地がある、WRITE は推敲が少ない、など。」

（Leader を指して）「LEADER が 3 領域を横断して『次のおすすめ』を出します。」

### 0:50–2:10 CODE を 1 問 — 誤答 → 一段ヒント → 正答

**操作**: アドレスバーで `/learn/code?task=code-003` を開く（「平均値の計算」のバグ発見問題。決定論的に採点される）。

**話す**: 「では実際に 1 問。この関数は平均値を返すはずですが、期待通り動かない。実際の出力は？」

**操作**: わざと `4.0` と入力して「答える」。

**見せる**: 「その答えでは正解になりません」＋ **hint 1**「return の式をよく見てください。演算子の優先順位はどうなっていますか？」

**話す**: 「答えは教えない。問い返しが一段だけ。ここが設計の肝です。ヒントは最大 3 回で、何回目のヒントで解けたかがそのまま証拠になります。」

**操作**: `3.0` と入力 →「答える」。

**見せる**: 「正解（ヒント 1 回）」、解説、**CODE 79 → 80**（seed 直後の値。ヒント 1 回の成功 = 0.8 は現在の平均よりわずかに高いので 1 上がる）、Achievement「立て直し」解除、Leader の寸評に「直近では CODE『バグ発見: 平均値の計算』（難易度 3）をヒント 1 回で解決。」が加わり、次のおすすめが WRITE になる。

**話す**: 「この 1 回の行動が learning event として記録されて、CODE の subskill（デバッグ・トレース）が再計算され、LEADER の総合寸評と次の課題が更新されました。数値はほぼ動かない — ヒント付きの成功は『ヒントなし成功』より弱い証拠だからです。一回で大きく動かないこと自体が設計です。」

> 補足: もう 1 回わざと間違えてから正解すると（ヒント 2 回 = 0.6）、CODE は 79 → 78 と **下がります**。それでも構わなければ「正解しても下がる。証拠ベースとはそういうこと」という説明に使えますが、基本は誤答 1 回で。

### 2:10–2:30 Dashboard に戻る — 三角形が動いた

**操作**: 「Dashboard で変化を見る」。

**話す**: 「三角形の CODE 頂点がわずかに動き、最近の学習履歴の先頭に今の 1 問が『成功 /h1』として載っています。Achievement『立て直し』（誤答のあとヒントで正解）も解除。」

### 2:30–2:55 LINE — 入口としての Leader（連携済み）

**操作**: スマートフォンで LINE を開き、「10分くらい何かやりたい」と送る。

**見せる**: 「10分あれば1〜2問いけます。WRITE: 「明確さ」を扱う難易度3の課題を1問」＋ Web へのボタン。タップすると WRITE の課題ページが開く。

**話す**:
「LINE には LEADER だけが出ます。軽い会話と曖昧な要求への提案、Web への誘導が役割で、課題そのものは Web でやる。

この LINE は Web アカウントと連携してあります。連携は LINE で『連携』と送ると 15 分だけ有効なワンタイム URL が届いて、Google ログイン中に『連携する』を押すと紐づく仕組みです。連携後は、いまのように**実際の学習記録にもとづいた提案**が返ります。」

**（時間が余れば）操作**: LINE で「プロフィール」と送る。

**見せる**: 「READ 72 / WRITE 57 / CODE 79」＋総合寸評。

**話す**: 「Web で解いた結果がそのまま LINE 側にも出ます。LINE に渡しているのは学習記録の集計だけで、氏名やメールは渡していません。」

### 締め（10 秒）

「skills are local, learner is global。領域ごとの記憶と、それを束ねる学習者モデル。AI 層は Dify を server-side から呼んでいて、落ちてもルールベースにフォールバックするので、デモも運用も止まりません。」

## 質問が来たときの補足

| 質問 | 答え |
|---|---|
| スコアはどう計算？ | `src/lib/scoring.ts`。基礎点 × 難易度重み × 新しさ重み、事前分布で緩和。IRT は 24h MVP では未実装 |
| AI は何をしている？ | 寸評・観察・次のおすすめ・ヒントの選択（解釈）。採点は選択/短答は決定論、記述はルーブリック＋AI |
| AI が答えを書いてしまわない保証は？ | system policy 7 か条＋ヒントは事前に段階設計されたものから一段だけ。決定論採点が確定しているときは AI の status を上書き |
| 個人情報は？ | 保存は表示名とメールのみ。Dify には内部 UUID だけ渡す。API key は全部 server-side |
| Dify が落ちたら？ | `LearningAIService` が Mock provider にフォールバック。`/api/health` の `ai.lastUsed` で確認可 |
| LINE と Google は紐づく？ | LINE で「連携」→ ワンタイム URL（単回・15 分）→ Web でログイン中に承認、で紐づく。「連携解除」でいつでも解除。連携しなくても LINE 単体で使える |
| 問題数は？ | READ 12 / WRITE 12 / CODE 15 の計 39 問。全 subskill を 2 問以上でカバー |

## 壊れたときの逃げ道

| 事象 | 対応 |
|---|---|
| Google ログイン失敗 | `/login` の「デモとして入る」（`DEMO_LOGIN_ENABLED=true` のとき） |
| 三角形が空 | Dashboard の「デモデータを投入」 |
| `code-003` が「回答済み」で別の問題が出る | URL に `?task=code-003` を付ければ指定できる |
| AI の返答が遅い | Dify のタイムアウトは 20 秒 → 超えると Mock に切替。待てないなら `AI_PROVIDER=mock` で再デプロイ |
| LINE が返事しない | Web 側だけで完結させ、「LINE は入口」の説明はスクリーンショットで代替 |
| LINE の連携が切れている | LINE で「連携」と送り直す（15 分有効）。時間が無ければ未連携のまま見せる（提案はルールベースに切り替わるだけで動く） |
